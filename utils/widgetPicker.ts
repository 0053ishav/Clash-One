// import { NativeModules } from "react-native";
// import { track } from "./analytics/analytics";

// export const addWidget = (type: "builder" | "multi" | "lab" | "pet") => {
//     track("widget_add_triggered", { type });

//     NativeModules.WidgetPicker?.addWidget(type);
// };

// export const updateWidgetAccess = (isPro: boolean) => {
//         track("widget_pro", { isPro });

//   NativeModules.WidgetPicker?.updateWidgetAccess(isPro);
// };